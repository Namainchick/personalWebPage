/**
 * The desktop. The Terminal window opens from the store's initial state (pathname "/"),
 * so this page renders nothing: closing the last window must leave an empty desktop.
 */
export default function Home() {
  return null;
}
